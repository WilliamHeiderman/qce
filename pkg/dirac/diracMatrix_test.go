package diracMatrix

// func TestNewSuccess(t *testing.T) {
// 	m1 := NewDiracMatrix()
// 	log.Println(m1)
// 	assert.Equal(t, &Matrix{}, m1, "New() returns a matrix pointer.")
// 	assert.Equal(t, [4][4]int{}, m1.Value, "Initial Value is a DiracMatrix.")
// 	assert.Equal(t, 0, m1.Size, "Initial Size is 0.")
// }
